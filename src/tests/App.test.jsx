
//The first test "render header" checks if there is a header component in the browser, 
//while the second test "render home page" checks if the home page is rendered.

/*
import { render, screen } from '@testing-library/react';
import App from './App';

test('renders header', () => {
  render(<App />);
  const headerElement = screen.getByRole('banner');
  expect(headerElement).toBeInTheDocument();
});

test('renders home page', () => {
  render(<App />);
  const homePageElement = screen.getByRole('main');
  expect(homePageElement).toBeInTheDocument();
});

*/